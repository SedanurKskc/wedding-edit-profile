import 'package:flutter/material.dart';

class CameloottLoader extends StatelessWidget {
  const CameloottLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: CircularProgressIndicator());
  }
}
